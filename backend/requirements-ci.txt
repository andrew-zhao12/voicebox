# The torch-free environment CI uses for ruff and the tests that build the
# security stub app or exercise the queue (see .github/workflows/ci.yml and
# backend/ruff-clean-paths.txt).  Nothing here loads a model.
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
sqlalchemy>=2.0.0
httpx>=0.27.0
numpy>=1.24.0,<2.0
python-multipart>=0.0.6
soundfile>=0.12.0
pedalboard>=0.9.0
pytest
pytest-asyncio
ruff
