# Voicebox — CPU build (default)
# GPU overlays:
#   NVIDIA: docker compose -f docker-compose.yml -f docker-compose.cuda.yml up --build
#   AMD:    docker compose -f docker-compose.yml -f docker-compose.rocm.yml up --build
# TLS in front (Caddy): add -f docker-compose.tls.yml (see deploy/Caddyfile).

services:
  voicebox:
    build: .
    container_name: voicebox
    restart: unless-stopped
    # Long enough for uvicorn's 40 s graceful window plus the queue drain.
    stop_grace_period: 45s

    ports:
      # Host-side moved to 17600 so the dev/installed Voicebox can keep 17493.
      # Container still listens on its native port internally.
      - "127.0.0.1:17600:17493"

    volumes:
      # Bind-mount for generated audio (customize the host path as needed)
      # Host side:      ./output/
      # Container side: /app/data/generations/
      - ./output:/app/data/generations

      # Named volume for profiles, DB, cache (persists across container restarts)
      - voicebox-data:/app/data

      # HuggingFace model cache (so models aren't re-downloaded on rebuild)
      - huggingface-cache:/home/voicebox/.cache/huggingface

    environment:
      - LOG_LEVEL=info
      - NUMBA_CACHE_DIR=/tmp/numba_cache
      # Models to download and load at boot; /health/ready answers 503 until
      # they are resident (names from `python -m backend.preload --list`).
      - VOICEBOX_PRELOAD_MODELS
      # Delete generations and captures older than N days, daily. Unset = keep.
      - VOICEBOX_RETENTION_DAYS
      # Seconds a shutdown waits for queued generations (default 30).
      - VOICEBOX_DRAIN_TIMEOUT_S
      # Every endpoint needs an API key. Export VOICEBOX_API_KEY on the host
      # (e.g. `export VOICEBOX_API_KEY=$(openssl rand -base64 32)`) to set the
      # admin key; leave it unset and the server creates one in the data
      # volume, readable with `docker compose exec voicebox cat /app/data/api_key`.
      - VOICEBOX_API_KEY
      # Set to 1 to remove the interactive API docs on a headless server.
      - VOICEBOX_DISABLE_DOCS
      # Comma-separated browser origins that may call the API directly.
      - VOICEBOX_CORS_ORIGINS

    security_opt:
      - no-new-privileges:true

    networks:
      - voicebox-net

    deploy:
      resources:
        limits:
          cpus: '4'
          # A cap, not a reservation: Qwen 1.7B on CPU plus Whisper needs more than 8G.
          memory: 16G

networks:
  voicebox-net:
    driver: bridge

volumes:
  voicebox-data:
  huggingface-cache:
